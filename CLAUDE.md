# CLAUDE.md — 얼마받지 (benefit-finder)

새 세션에서 이 저장소를 처음 여는 사람(또는 AI)이 **읽자마자 작업 가능**하도록 만든 문서.
프로젝트 고유의 함정과 규칙만 적는다. 일반적인 Astro 사용법은 적지 않는다.

---

## 1. 이 사이트가 무엇인가

**얼마받지** — 한국 정부지원금 예상 수령액을 계산해 주는 **민간 정보 사이트**.
구글 애드센스 수익이 목적인 부업 프로젝트다. 정부기관이 아니고, 신청 대행도 하지 않는다.

- 위치: `/Users/shon/Desktop/benefit-finder`
- 스택: **Astro 4.16** 정적 사이트. React 없음, 서버 없음, DB 없음.
- 배포: GitHub Pages (`https://seongho-son.github.io`)
- 사용자의 **다른** 애드센스 사이트로 `~/Desktop/sallim-note`(살림노트)가 있다. **완전히 별개 프로젝트다. 혼동 금지.**

### 성격이 YMYL이다 — 이게 가장 중요하다
돈·자격을 다루므로 틀린 숫자가 곧 사용자 피해다. 이 저장소에서 **가장 위험한 실수는 코드 버그가 아니라 낡은 제도값**이다.
정부지원금 기준은 매년(때로는 분기마다) 바뀌는데 코드는 스냅샷이라, "한 번 맞았던 값"이 조용히 틀린 값이 된다.

---

## 2. 명령어

```bash
npm i           # 의존성
npm run dev     # 개발 서버
npm run build   # 프로덕션 빌드 → dist/
npm run check   # astro check (타입/템플릿 검증)
npm run preview # 빌드 결과 미리보기
```

테스트 프레임워크는 없다. 계산 로직 검증은 **스크래치패드에 `<script>` 내용을 발췌 복사해 `node`로 실행**하는 방식으로 한다(아래 5절).

---

## 3. 절대 하면 안 되는 것 (실제로 사고가 났던 것들)

### 🚫 `@astrojs/sitemap`을 3.7.x로 올리지 마라
`package.json`에 **3.2.1로 고정**되어 있다. Astro 4의 `astro:build:done` 라우트 API와 불일치해 **빌드가 크래시**한다.
`npm update`나 `npm audit fix`가 조용히 올려버릴 수 있으니 주의.

### 🚫 계산기에 라이브러리를 추가하지 마라
계산기는 전부 페이지 하단 `<script>` 안의 **vanilla JS**다. React/차트/날짜 라이브러리 전부 금지.
정적 사이트의 가벼움 + 애드센스 로딩 속도가 이 프로젝트의 전제다.

### 🚫 사용자 입력을 외부로 보내지 마라
소득·재산·급여·출생일 등 민감 정보를 입력받는다. 현재 `fetch` / `localStorage` / `sessionStorage` / analytics / `form action`이
**저장소 전체에 0건**이며, 이건 의도된 설계다. `Disclaimer` 컴포넌트가 "개인정보를 수집하지 않고 모든 계산은 브라우저 안에서만"이라고
**명시적으로 약속**하고 있으므로, 추적 코드를 넣는 순간 그 고지가 거짓이 된다.
GA/GTM 도입이 필요하면 반드시 `privacy.astro` 고지와 `Disclaimer` 문구를 함께 고쳐야 한다.

### 🚫 게시자 ID·이메일을 지어내지 마라
`public/ads.txt`의 `pub-XXXX`와 `contact.astro`의 이메일은 **사용자만 아는 값**이다. 플레이스홀더로 남겨두고 사용자에게 물어라.

### 🚫 OG 이미지에 정부 상징을 쓰지 마라
태극·무궁화·부처 로고를 쓰는 순간 정부기관 사칭 이슈가 된다. 사이트 전체가 "우리는 정부기관이 아니다"를 반복 고지하는 구조로
설계돼 있으므로 이를 훼손하면 안 된다.

---

## 4. 구조

```
src/
  components/
    Disclaimer.astro   ← YMYL 면책 + 정부 사칭 방지. 계산기/정보 페이지에 필수 삽입
    Header.astro  Footer.astro
  layouts/BaseLayout.astro   ← meta/OG, AdSense 스크립트 주입 지점(ADSENSE_CLIENT)
  pages/
    index.astro  about.astro  contact.astro  privacy.astro  disclaimer.astro
    blog/index.astro  blog/[...slug].astro
    tools/                      ← 계산기 4종. 이 사이트의 핵심 자산
      labor-tax-credit.astro    근로장려금
      parenting-benefit.astro   출산·육아 지원금 총액
      unemployment-benefit.astro 실업급여
      youth-rent.astro          청년월세
  content/blog/*.md             ← 콘텐츠 클러스터(애드센스 승인용)
  styles/global.css             ← 공용 클래스. 페이지들이 공유하므로 수정 시 파급 주의
public/ads.txt
CONTENT-PLAN.md                 ← 콘텐츠 20개 클러스터 기획서
HANDOFF-B-parenting-calculator.md ← 계산기 B 작업지시서(⚠️ 원본에 제도값 오류가 있었음, 4절 참고)
```

### 계산기 페이지의 고정 패턴
새 계산기를 만들 때는 **기존 파일을 그대로 따라야** 한다:

1. `BaseLayout` + `Disclaimer` import
2. `<Disclaimer source="..." updated="YYYY-MM-DD" />` 를 계산기 **위**에 배치
3. `global.css`의 기존 클래스 재사용: `.calc` `.result` `.result.hidden` `.total` `.pills` `.disclaimer` `.ad-slot`
   → **새 CSS는 최소화.** 페이지 전용 스타일이 꼭 필요하면 `global.css`가 아니라 **페이지 내 `<style>`**에 넣어라
   (global.css는 전 페이지 공유라 한 페이지 사정으로 고치면 다른 페이지가 깨진다)
4. 계산 로직은 페이지 하단 `<script>` 안 vanilla JS
5. 결과 영역은 기본 `hidden`, 계산 버튼 클릭 시 노출
6. 하단에 설명 본문(H2 여러 개) + `.ad-slot` + 관련 글 내부 링크
   → 애드센스 "가치 있는 콘텐츠" 확보용이므로 **본문을 빼면 안 된다**

---

## 5. 제도값을 고칠 때의 필수 절차

이 저장소에서 가장 자주 하게 될 작업이고, 가장 많이 실수가 난 지점이다.

### ① 상수는 반드시 전수 grep으로 찾아라
같은 숫자가 **여러 곳에 따로 하드코딩**되어 있다. 상수 1곳만 고치면 화면 안에서 서로 모순된다.
> 실제 사례: `parenting-benefit.astro`의 아동수당 `96`개월이 JS 상수 / `input max="95"` / 안내문 /
> 결과 헤더 "만 8세까지" / 본문 표 2곳 / "약 2,960만원" 총 **7곳**에 별도 문자열로 존재했다.

```bash
grep -n "96\|95\|8세\|2,960" src/pages/tools/parenting-benefit.astro   # 이런 식으로 전수 확인
```
가능하면 Astro 프론트매터에 상수를 정의하고 본문에서 `{}`로 참조해 재발을 막아라.

### ② 공식 1차 출처로 확인하라. 블로그·요약 사이트를 근거로 삼지 마라
- 국세청 `nts.go.kr`, 법령은 `law.go.kr` / `taxlaw.nts.go.kr`(조특법 시행령 별표11 등)
- 고용노동부 `moel.go.kr`, 고용24 `work24.go.kr`, 찾기쉬운 생활법령 `easylaw.go.kr`
- 보건복지부 `mohw.go.kr`, 복지로 `bokjiro.go.kr`, 정책브리핑 `korea.kr`
- ⚠️ **고용노동부 FAQ 페이지는 갱신이 늦다.** 실제로 실업급여 상한액이 개정됐는데 FAQ는 옛 값(66,000원)을 그대로 두고 있었다.
  보도자료·시행령 개정문으로 교차검증할 것.
- 출처를 못 찾으면 **"일치"라고 단정하지 말고 "확인 필요"로 남겨라.** 추측으로 숫자를 쓰는 게 최악이다.

### ③ 값을 고쳤으면 `Disclaimer`의 `source`/`updated`를 반드시 함께 갱신하라
가장 위험한 상태는 **낡은 값에 "2026년 기준, 최종 확인 오늘" 도장이 찍혀 있는 것**이다.
`source`에는 기관명만 쓰지 말고 **실제 근거(고시번호·법령·시행일)**를 적는다.

```astro
<!-- 나쁨 -->
<Disclaimer source="국토교통부·복지로, 2026년 기준" updated="2026-07-27" />
<!-- 좋음 -->
<Disclaimer source="고용노동부고시 제2025-47호·고용보험법 시행령(2026.1.1. 시행)" updated="2026-07-27" />
```
값을 못 고칠 상황이면 `updated`부터 내려라. 틀린 값보다 "최신임을 주장하는 틀린 값"이 훨씬 나쁘다.

### ④ 검증은 node로 직접 돌려라
`<script>` 내용을 스크래치패드에 복사해 대표 입력 6~8개(**경계값 포함**: 구간 시작/끝, 0, 음수, 상한 초과)를
손계산과 대조한다. 원본 파일에서 실험하지 말 것.

### ⑤ 미검증 요건을 "충족"으로 처리하지 마라
판정을 건너뛴 항목(`skip`)이 불합격에 안 잡히면, 실제로는 부적격인 사람에게 "요건 충족"이 나온다.
**낙관 편향은 구조로 고정되기 쉽다.** 검증하지 않은 요건은 `— 미확인`으로 명확히 구분 표기하고,
결과 카드 안에 "이 항목은 미반영"을 상시 노출하라.

---

## 6. 애드센스 관련 규칙

- `.ad-slot`과 **콘텐츠 CTA는 클래스를 공유하면 안 된다.** 광고를 콘텐츠처럼 보이게 하는 배치는 계정 정지 사유다.
- 광고 슬롯을 계산 버튼·결과 카드 바로 아래 붙이지 마라. 오클릭을 유발한다. 여백을 넉넉히 둔다.
- "여기를 클릭" 류 클릭 유도 문구 금지.
- 블로그 글은 **pubDate를 같은 날로 몰지 마라.** 21편이 전부 같은 날짜 + 동일 골격 + 균일한 분량이면
  "프로그램적으로 찍어낸 얇은 콘텐츠"로 판정될 수 있다.
- 각 글은 1,000자 이상, 모든 수치에 출처+기준연도+최종확인일. **"무조건 받는다" 같은 확정 단정 금지**(`CONTENT-PLAN.md` 규약).
- EEA/UK 트래픽에는 인증 CMP(쿠키 동의)가 **의무**다. AdSense 실가동 전에 Funding Choices 등 연동이 필요하다.

---

## 7. 배포 — ⚠️ 함정이 있다

- 저장소: `github.com/seongho-son/seongho-son.github.io`, 기본 브랜치 `main`
- 원격에 `main`과 **`gh-pages`** 두 브랜치가 있다.
- `.github/workflows/deploy.yml`이 로컬에 존재하지만 **`.gitignore`에 `.github/workflows/`가 등록돼 있어 커밋되지 않는다.**
  `.gitignore`의 주석: *"workflow는 OAuth workflow 스코프 없어 푸시 불가 — gh-pages 브랜치로 수동 배포"*
  → 즉 **GitHub Actions 자동 배포가 실제로 돌고 있다고 가정하지 마라.** 배포는 gh-pages 브랜치 수동 반영 경로다.
  배포 방식을 바꾸려면 먼저 사용자에게 현재 어떻게 올리고 있는지 확인할 것.
- `astro.config.mjs`의 `site`는 루트 사용자 사이트라 `base`가 `/`다. 커스텀 도메인 연결 시 `site` 값만 바꾸면 된다.

### 커밋·푸시
**사용자가 명시적으로 지시할 때만 한다.** "고쳐줘"는 커밋 지시가 아니다. 기본은 워킹트리에 두고 보고만.

---

## 8. 알려진 미해결 사항 (2026-07-27 기준)

- **근로장려금 점증 구간**: 실제 산정표는 **계단식**(구간별 상한액 기준)인데 코드는 연속 직선 근사다.
  점증 구간에서 최대 약 4만원 과소 산정된다. 정확히 하려면 조특법 시행령 별표11 구간표 전체 이식이 필요하다(별도 작업).
  현재는 결과 영역에 오차 가능성을 고지하는 것으로 처리했다.
- **아동수당 연령 확대**: 2030년까지 만 13세 미만으로 매년 1세씩 확대 예정이라, 오늘 태어난 아이의 *생애* 총액은
  현재 계산값보다 커질 수 있다. 코호트별 적용 규칙은 공식 확정 자료가 불명확해 "2026년 현재 기준"으로 한정 표기했다.
- **실업급여 반복수급 감액**(5년 내 3회 이상 시 10~50% 감액, 대기기간 최대 4주)이 **2026-08-20 시행 예정**이다.
  계산기 미반영 상태이므로 시행일에 맞춰 반영 여부를 결정해야 한다.
- `public/ads.txt` 게시자 ID, `contact.astro` 이메일 → **사용자 입력 대기 중**.
- `HANDOFF-B-parenting-calculator.md`는 **원본 지시서 자체에 아동수당 제도값 오류**가 있었다.
  이 문서를 근거로 작업할 때 숫자를 그대로 믿지 말고 공식 출처로 재확인할 것.

## 글·문구를 쓸 때 (필수)

**`WRITING-GUIDE.md`를 먼저 읽는다.** 이 사이트의 존재 이유는 공공기관 문서를 일반인이 알아듣게
옮기는 것이므로, 관공서 용어(산정액·원가구·귀속·소득인정액 등)를 그대로 쓰면 안 된다.
용어 사전과 문장 규칙, 새 글 체크리스트가 그 문서에 있다.
