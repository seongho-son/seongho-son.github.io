# 작업 지시서 — 출산·육아 지원금 총액 시뮬레이터 (B)

다른 AI/개발자에게 이 문서 전체를 그대로 전달하세요. 이 저장소 안에서 완결되는 작업입니다.

> **📌 정정 메모 (2026-07-27 감사)**
> 이 지시서 2절의 아동수당 규칙이 **원본부터 틀렸습니다.** 아동수당 지급 연령은
> **2026년 4월 24일 시행으로 만 8세 미만(96개월) → 만 9세 미만(108개월)로 확대**되었습니다.
> 이에 따라 아동수당 생애 총액 960만원 → **1,080만원**, 첫째 총액 2,960만원 → **3,080만원**,
> 둘째 이상 3,060만원 → **3,180만원**으로 정정했습니다. (근거: 보건복지부 아동수당 확대 보도자료)
> 아울러 원본에 누락되어 있던 **시행 코호트(부모급여 2023년생부터 / 첫만남이용권 2022년생부터,
> 둘째 이상 300만원은 2024년생부터)**, **60일 소급 제한**, **첫만남이용권 2년 사용기한**,
> **부모급여↔가정양육수당 중복 불가** 규칙을 아래에 추가했습니다.
> 아동수당은 2030년까지 만 13세 미만으로 매년 1세씩 확대가 예고되어 있으나 코호트별 적용 기준이
> 확정되지 않았으므로, **"2026년 현재 기준 만 9세 미만"으로 한정 표기**할 것.

---

## 0. 컨텍스트 (반드시 읽을 것)

- 프로젝트: **얼마받지** — 정부지원금 예상 수령액을 계산해 주는 민간 정보 사이트 (구글 애드센스 부업).
- 위치: `/Users/shon/Desktop/benefit-finder`
- 스택: **Astro 4.16** 정적 사이트 (React 없음, 서버 없음), `@astrojs/sitemap@3.2.1` 고정.
  - ⚠️ sitemap을 3.7.x로 올리지 말 것 — Astro 4의 `astro:build:done` 라우트 API와 불일치해 빌드가 크래시함.
- 계산기는 전부 **클라이언트 사이드 vanilla JS** (`<script>` 태그). 개인정보를 절대 수집/전송하지 않음.

## 1. 만들 것

**파일:** `src/pages/tools/parenting-benefit.astro`
아이의 출생 시점(또는 현재 개월수)과 출생 순위를 입력하면 **출산·육아 지원금 총액과 남은 수령액, 타임라인**을 계산하는 페이지.

### 반드시 기존 패턴을 그대로 따를 것
**참고 파일(똑같은 구조로 만들 것):** `src/pages/tools/labor-tax-credit.astro`
- 상단에서 `BaseLayout`과 `Disclaimer` 컴포넌트 import
- `<Disclaimer source="..." updated="2026-07-27" />` 를 계산기 위에 배치
- `.calc` / `.result` / `.result.hidden` / `.total` / `.pills` / `.disclaimer` / `.ad-slot` 등 **기존 global.css 클래스 재사용** (새 CSS 최소화)
- 계산 로직은 페이지 하단 `<script>` 안의 vanilla JS (라이브러리 금지)
- 결과 영역은 기본 `hidden`, 계산 버튼 클릭 시 노출
- 페이지 하단에 설명용 본문(H2 여러 개)과 `.ad-slot`, 관련 글 내부 링크 추가 (애드센스 "가치 있는 콘텐츠" 확보용)

## 2. 계산 규칙 (2026년 기준 — 반드시 출처/기준연도 표기)

세 가지 지원을 합산한다. 모두 클라이언트에서 계산.

> ⚠️ 아래 개월수·금액은 **하드코딩하지 말고 `.astro` 프론트매터 상수로 한 번만 정의**하고
> 본문과 `<script>`(`define:vars`)에서 참조할 것. 원본 구현은 `96`/`95`/"만 8세"/"2,960만원"이
> 7곳에 흩어져 있어 제도 변경 시 누락이 발생했다.

### (1) 부모급여
- 만 0세 (0~11개월, 12개월): **월 100만원**
- 만 1세 (12~23개월, 12개월): **월 50만원**
- 만 2세 이후: 없음
- 생애 총액(만2년): 100만×12 + 50만×12 = **1,800만원**
- **시행 코호트: 2023년 출생아부터.** 그 이전 출생아는 받지 않았으므로 총액·기수령액에서 제외할 것.
- **가정양육수당과 중복 불가** (부모급여가 만 0~1세 가정양육수당을 대체). 본문에 반드시 명시.

### (2) 첫만남이용권 (출생 시 1회)
- 첫째아: **200만원**
- 둘째아 이상: **300만원**
- **시행 코호트: 2022년 출생아부터.** 단, **둘째 이상 300만원은 2024년 출생아부터**
  (2022~2023년 출생아는 둘째 이상도 200만원).
- 현금이 아닌 **국민행복카드 바우처**이며 **출생일로부터 2년 경과 시 잔액 소멸**.

### (3) 아동수당
- **만 9세 미만 (0~107개월, 108개월): 월 10만원** ← 2026-04-24 시행으로 확대(구: 만 8세 미만 96개월)
- 생애 총액: 10만×108 = **1,080만원**
- 비수도권 인구감소지역 등은 **월 5천~2만원 지역 가산**이 있으나 계산에 미반영 → 안내 문구로 고지.
- 2030년까지 만 13세 미만으로 추가 확대 예정이나 코호트별 적용 기준 미확정 → **2026년 기준으로만 표기.**

### (4) 공통 — 신청 소급 제한
- **출생일 포함 60일 이내 신청 시에만 출생월분까지 소급**, 이후 신청 시 **신청월분부터**.
- 따라서 계산 결과는 "전 기간 수급 가정 **예상 최대치**"로 라벨링하고 결과 영역에 이 제한을 명시할 것.
- 계산 규약상 **당월분은 '이미 받은 금액'에 포함하지 않음** — 결과 영역에 주석으로 밝힐 것.

### 총액 예시 (검산용, 반드시 이 값이 나와야 함 / 2023년 이후 출생아 기준)
- 첫째아 생애 총액: 1,800 + 200 + 1,080 = **3,080만원**
- 둘째아 이상 생애 총액: 1,800 + 300 + 1,080 = **3,180만원**

### 입력 → 출력
- **입력:** ① 출생 순위(첫째 / 둘째 이상) — `.pills` 라디오 / ② 현재 개월수(0~107, 숫자) 또는 출생연월
  - 빈값·음수·비숫자 입력은 **무음으로 숨기지 말고 오류 메시지를 노출**할 것.
  - 상한(107) 초과 입력도 **무음 클램프 금지** — 경고 문구를 함께 노출할 것.
- **출력:**
  - 생애 총 예상 수령액 (`.total`에 크게)
  - 항목별 내역 테이블 (부모급여 / 첫만남 / 아동수당)
  - 현재 개월수 기준 **이미 받았을 금액 / 앞으로 받을 금액** 분리 표시
  - (선택) 월별 타임라인 간단 표

> 지자체 출산장려금은 지역별로 천차만별이므로 **총액에 포함하지 말고**, "거주 지자체 별도 확인" 안내 문구만 넣을 것.

## 3. 컴플라이언스 (어기면 애드센스 계정 정지 위험 — 필수)

- [ ] 개인정보(이름·주민번호·계좌) 입력 요구 금지. 개월수/순위만.
- [ ] "얼마받지는 정부기관이 아니며 참고용" 고지 = `Disclaimer` 컴포넌트로 충족.
- [ ] 모든 금액에 기준연도+출처 표기. `source`에는 **실제 근거 문서**를 적을 것
      (예: `source="보건복지부 아동수당·부모급여 고시 및 복지로 안내(2026.4.24. 아동수당 만 9세 미만 확대 시행 반영), 2026년 기준"`).
      `"보건복지부, 2026년 기준"`처럼 근거를 알 수 없는 표기는 금지.
- [ ] "실제 지급은 심사·거주지에 따라 다를 수 있음" 면책 문구.
- [ ] 결과 화면에 공식 신청처 딥링크: 정부24(https://www.gov.kr), 복지로(https://www.bokjiro.go.kr).
- [ ] "신청 대행" 뉘앙스 금지 — 자가진단까지만.

## 4. 홈 화면 연결 (잊지 말 것)

`src/pages/index.astro` 의 `tools` 배열에서 아래 항목의 `ready: false` → **`ready: true`** 로 변경:
```js
{
  href: '/tools/parenting-benefit',
  emoji: '👶',
  title: '출산·육아 지원금 총액 계산기',
  desc: '부모급여+첫만남+아동수당 총액 시뮬레이션',
  ready: false,   // ← true 로 변경
},
```

## 5. 검증 (완료 조건)

```bash
cd /Users/shon/Desktop/benefit-finder
npm run build      # 에러 없이 "Complete!" + 페이지 수 증가 확인
```
- [ ] 빌드 성공 (page 수 8 → 9)
- [ ] 첫째아·107개월 이하로 넣었을 때 계산 결과가 위 "검산용" 총액과 일치
      (단, 2023년 이전 출생 추정 구간은 부모급여/첫만남이 제외되어 총액이 더 작게 나오는 것이 정상)
- [ ] 홈(`index.astro`) 카드에서 '준비 중' 뱃지 사라지고 링크 활성화
- [ ] `Disclaimer` + 공식 링크 + 출처 표기 포함

## 6. 하지 말 것
- React/Vue 등 프레임워크 추가 금지 (vanilla JS만)
- 새 npm 패키지 설치 금지
- sitemap 버전 변경 금지
- 서버/DB/외부 API 호출 추가 금지 (이 계산기는 순수 클라이언트 계산)
